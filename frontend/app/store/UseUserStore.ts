// import { create } from 'zustand'

// interface IUserstore {
//   name: number
//   increase: (by: number) => void
// }

// const useBearStore = create<IUserstore>()((set) => ({
//   bears: 0,
//   increase: (by) => set((state) => ({ bears: state.bears + by })),
// }))
